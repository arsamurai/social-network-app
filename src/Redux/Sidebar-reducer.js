let initializationState = {
   title: ['Profile', 'Messages', 'News', 'Music', 'Find users', 'Settings']
}

const sidebarReducer = (state = initializationState, action) => {
   return state;
}

export default sidebarReducer;